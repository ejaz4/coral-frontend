
/* Set up your strings here, it's recommended if you want multilingual language support. */
var flsestrings = {
    "title":{
        "default": "Set a default title here",
        "es": "Définissez un titre par défaut ici"
    },
    "anotherstring":{
        "default":"This is another string!"
    }
}

function loadcall(){
    resize();
    /* JS to run after page loaded, you can still run JS normally. */    
}